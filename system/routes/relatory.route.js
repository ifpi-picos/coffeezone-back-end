module.exports = class RelatoryRoute {
    constructor(app) {
        const { Router } = require('express');
        const routes = Router()

        


        return { path: '/relatory/', router: routes }
    }
}