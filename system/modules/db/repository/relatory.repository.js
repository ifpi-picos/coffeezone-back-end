const { relatorys } = require('../models')

module.exports = class RelatorysRepository {
    constructor(){
        return{
            name: "relatorys",
            functions: {
            }
        }
    }
}